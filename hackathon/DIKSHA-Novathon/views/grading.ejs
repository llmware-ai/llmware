<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Diksha</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">

    <!-- Favicon -->
    <link href="/img/favicon.ico" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="/lib/animate/animate.min.css" rel="stylesheet">
    <link href="/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="/css/style.css" rel="stylesheet">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
            background-color: #d5c5f1;
            font-family: Arial, sans-serif;
        }
        /* Fix navbar visibility */
        /* Ensure the navbar is fixed on top */
.navbar {
    position: sticky;
    top: 0;
    z-index: 1000;
}

/* Optional: Ensure the navbar items are displayed in a row */
.navbar-nav {
    display: flex;
    align-items: center;
    justify-content: center;
}

/* For proper mobile responsiveness */
.navbar-toggler {
    border: 1px solid #ccc;
}

/* Apply a background color for visibility */
.navbar-light {
    background-color: #fff;
}

/* Optional: Change text color on hover for navbar links */
.navbar-nav .nav-link:hover {
    color: #FFBE00;
}

        nav.navbar {
            background-color: #fff; /* White background */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Slight shadow for contrast */
            padding: 10px 20px; /* Added padding for better spacing */
        }

        nav.navbar .navbar-brand {
            font-size: 24px;
            color: #333; /* Dark color for the navbar brand */
        }

        .navbar-nav a {
            color: #333 !important; /* Ensure nav links are dark enough */
            padding: 10px 15px;
            text-transform: uppercase;
        }

        .navbar-nav a:hover, .navbar-nav .active {
            background-color: #FFBE00; /* Highlight on hover and active links */
            color: white; /* White text on active links */
        }

        /* Ensure that the dropdown items are visible */
        .dropdown-menu {
            background-color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* Add a little more margin on top of the content for spacing */
        h1 {
            margin-top: 60px;
            color: #333;
        }

        table {
            border-collapse: collapse;
            margin: 20px 0;
            width: 90%;
            background-color: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 10px;
            text-align: center;
            background-color: #f9f9f9;
        }

        th {
            background-color: #f2f2f2;
            font-weight: bold;
        }

        td {
            background-color: white;
        }

        tr:nth-child(even) td {
            background-color: #f9f9f9;
        }

        tr:hover td {
            background-color: #FFBE00;
            color: white;
        }

        caption {
            margin-bottom: 10px;
            font-size: 18px;
            font-weight: bold;
        }

        input[type="text"] {
            width: 85%;
            padding: 5px;
            border-radius: 8px;
            border: 1px solid #ddd;
            box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        input[type="submit"] {
            background-color: #FFBE00;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            font-size: 16px;
            border-radius: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease-in-out;
        }

        input[type="submit"]:hover {
            background-color: #FFA500;
            transform: scale(1.05);
        }

        
    </style>
</head>
<body>
    <!-- Navbar Start -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow sticky-top p-0">
        <a class="navbar-brand d-flex align-items-center px-4 px-lg-5" href="/index">
            <h2 class="m-0 text-primary"><i class="fa fa-book me-3"></i><%= AppName %></h2>
        </a>
        <button class="navbar-toggler me-4" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav ms-auto p-4 p-lg-0">
                <li class="nav-item"><a href="/index" class="nav-link active">Home</a></li>
                <li class="nav-item"><a href="/about" class="nav-link">About</a></li>
                <li class="nav-item"><a href="/grading" class="nav-link">Grading System</a></li>
                <li class="nav-item"><a href="/practice" class="nav-link">Practice</a></li>
                <li class="nav-item"><a href="/analyze-pdf" class="nav-link">Analyze PDF</a></li>
                <li class="nav-item"><a href="/courses" class="nav-link">AI Services</a></li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Pages</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="/team">Our Team</a></li>
                        <li><a class="dropdown-item" href="/testimonial">Testimonial</a></li>
                    </ul>
                </li>
                <li class="nav-item"><a href="/contact" class="nav-link">Contact</a></li>
            </ul>
            <a href="/logout" class="btn btn-primary py-4 px-lg-5 d-none d-lg-block">Log Out <i class="fa fa-arrow-right ms-3"></i></a>
        </div>
    </nav>
    

    <h1 >Student Report Card</h1>
    <table>
        <caption>Enter Student Details</caption>
        <tr>
            <th colspan="2">Student Name:</th>
            <td colspan="2"><input type="text" id="studentName" placeholder="Enter Student Name"></td>
        </tr>
        <tr>
            <th>Subject</th>
            <th>Marks</th>
            <th>Credits</th>
            <th>Letter Grade</th>
        </tr>
        <tr>
            <td>English</td>
            <td><input type="text" id="englishMarks" placeholder="Marks"></td>
            <td><input type="text" id="englishCredits" placeholder="Credits"></td>
            <td><input type="text" id="englishLetter" placeholder="Grade"></td>
        </tr>
        <tr>
            <td>Physics</td>
            <td><input type="text" id="physicsMarks" placeholder="Marks"></td>
            <td><input type="text" id="physicsCredits" placeholder="Credits"></td>
            <td><input type="text" id="physicsLetter" placeholder="Grade"></td>
        </tr>
        <tr>
            <td>Chemistry</td>
            <td><input type="text" id="chemistryMarks" placeholder="Marks"></td>
            <td><input type="text" id="chemistryCredits" placeholder="Credits"></td>
            <td><input type="text" id="chemistryLetter" placeholder="Grade"></td>
        </tr>
        <tr>
            <td>Maths</td>
            <td><input type="text" id="mathsMarks" placeholder="Marks"></td>
            <td><input type="text" id="mathsCredits" placeholder="Credits"></td>
            <td><input type="text" id="mathsLetter" placeholder="Grade"></td>
        </tr>
        <tr>
            <td colspan="4">
                <input type="submit" value="Calculate" onclick="calculateScore()">
            </td>
        </tr>
    </table>

    <table id="SubjectDetails">
        <caption>Individual Subject Details</caption>
        <tr>
            <th>Subject</th>
            <th>Marks</th>
            <th>Status</th>
        </tr>
        <tbody id="subjectDetailsBody"></tbody>
    </table>

    <table id="TableScore">
        <caption>Final Result</caption>
        <tr>
            <th>Name</th>
            <th>Total Marks</th>
            <th>Average Marks</th>
            <th>Overall Remarks</th>
        </tr>
        <tbody id="finalResultBody"></tbody>
    </table>

    <script>
        function calculateScore() {
            const name = document.getElementById("studentName").value.trim();
            if (!name) {
                alert("Please enter the student's name.");
                return;
            }
    
            const subjects = [
                { name: "English", marks: parseFloat(document.getElementById("englishMarks").value) || 0 },
                { name: "Physics", marks: parseFloat(document.getElementById("physicsMarks").value) || 0 },
                { name: "Chemistry", marks: parseFloat(document.getElementById("chemistryMarks").value) || 0 },
                { name: "Maths", marks: parseFloat(document.getElementById("mathsMarks").value) || 0 }
            ];
    
            const subjectDetailsBody = document.getElementById("subjectDetailsBody");
            subjectDetailsBody.innerHTML = ""; // Clear previous rows
    
            let totalMarks = 0;
            let hasFailed = false;
    
            subjects.forEach((subject) => {
                const status = subject.marks >= 33 ? "Pass" : "Fail";
                const color = status === "Pass" ? "green" : "red";
    
                if (status === "Fail") {
                    hasFailed = true; // Mark the student as failed overall if any subject is failed
                }
    
                totalMarks += subject.marks;
    
                const row = `
                    <tr>
                        <td>${subject.name}</td>
                        <td>${subject.marks}</td>
                        <td style="color: ${color};">${status}</td>
                    </tr>
                `;
                subjectDetailsBody.innerHTML += row;
            });
    
            const averageMarks = totalMarks / subjects.length;
            const overallRemarks = hasFailed ? "Fail" : "Pass";
    
            const finalResultBody = document.getElementById("finalResultBody");
            finalResultBody.innerHTML = `
                <tr>
                    <td>${name}</td>
                    <td>${totalMarks.toFixed(2)}</td>
                    <td>${averageMarks.toFixed(2)}</td>
                    <td style="color: ${overallRemarks === "Pass" ? "green" : "red"};">${overallRemarks}</td>
                </tr>
            `;
        }
    </script>
    <!-- Bootstrap JS and Popper.js -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>

    <!-- Your other content follows... -->
</body>
</html>
